export function gatewayOffCopy(): string {
  return "Local gateway off. No receivable address is displayed.";
}

export function gatewayIssuingCopy(): string {
  return "Issuing a local textest intent. Nothing is receivable until a loopback gateway answers.";
}

export function gatewayUnavailableCopy(): string {
  return "Local gateway unavailable. No receivable address is displayed.";
}

export function gatewayIssuedCopy(): string {
  return "Testnet TEX issued for this session intent. Not mainnet, not minted credit.";
}
